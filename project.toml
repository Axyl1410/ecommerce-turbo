[build]
  command = "cd apps/express && pnpm build"

[deploy]
  release = "cd ../.. && pnpm --filter @workspace/database run database:generate && pnpm --filter @workspace/database run database:deploy"

